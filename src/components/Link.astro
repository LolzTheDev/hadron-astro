---
const { img, dest, name } = Astro.props

---

<button onclick={`goto("${dest}")`} class="p-6 w-[160px] hover:scale-[102%] duration-200 cursor-pointer rounded-lg bg-zinc-800 border border-zinc-600">
  <img class="size-16 mx-auto mb-4 rounded-md border border-zinc-600" src={`/assets/${img}`} />
  <p class="font-bold text-lg">{name}</p>
</button>
